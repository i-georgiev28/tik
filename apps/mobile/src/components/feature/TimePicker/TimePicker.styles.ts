import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  label: {
    // fontSize: 48,
    // marginHorizontal: 5,
    // marginBottom: 10,
              fontSize: 48, marginHorizontal: 5, marginBottom: 10, color: '#fff'

  },
  pickerContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
                color: '#fff',
            fontSize: 48,
  }
});
